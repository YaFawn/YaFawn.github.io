---
title: "Dashboard"
output: 
  flexdashboard::flex_dashboard:
    orientation: columns
    vertical_layout: fill
---

```{r setup, include=FALSE}
library(tidyverse)
library(p8105.datasets)
library(plotly)
library(flexdashboard)
```

```{r data manipulation, include=FALSE}
# randomly pick a sample of size 800 to better draw plotly graphs
data("instacart")
insta_df = instacart %>% 
  janitor::clean_names() %>% 
  sample_n(800)
```

Column {data-width=650}
-----------------------------------------------------------------------

### Bar Graph

```{r}
insta_df %>% 
  count(aisle) %>% 
  arrange(-n) %>% 
  mutate(aisle = fct_reorder(aisle, n, .desc = TRUE)) %>% 
  plot_ly(
    x = ~aisle, y = ~n, type = "bar", color = ~aisle, colors = "viridis"
  ) %>% 
  layout(title = "Items ordered in each aisle",
         xaxis  = list(title = "Aisle name"),
         yaxis  = list(title = "Items ordered")
         )
```

Column {data-width=350}
-----------------------------------------------------------------------

### Chart B

```{r}

```

### Chart C

```{r}

```

